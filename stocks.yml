config:
  target: 'http://localhost:3111'
  phases:
    - duration: 60
      arrivalRate: 400
 
scenarios:
  - name: 'Stock increment search'
    flow:
    - get:
        url: "/api/stocks/{{ $randomNumber(1,10000000) }}"

